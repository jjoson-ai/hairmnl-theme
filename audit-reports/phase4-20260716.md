# Phase 4 Closeout Audit — Pipeline 8 migration (epic 2i8b)

**Date:** 20260716 · **Baseline:** ba6c080 (PR #6, 2026-05-17) · **HEAD:** 32eb775 · **Scope:** 245 theme files (+91,227/−12,791 across 323 total)

**Method:** Fable 5 Ultracode multi-agent audit (per bd 2i8b.10; run in-CC per the do-not-offload-closeouts rule — the OC epic-audit pipeline's report/tag contract is honored). 12 area auditors + 4 cross-cutting lenses over the full diff at HEAD; every medium+ finding adversarially verified (default-refute), critical/high survivors independently double-voted; completeness critic over coverage; 46 agents, 1,038 tool uses, ~4.15M subagent tokens, 0 agent errors. Framing per ticket: fix regressions / incomplete fixes / interaction effects across waves — not re-litigating triaged findings.

## Verdict

GO-WITH-FIXES. No critical findings: nothing breaks the purchase path server-side or page render, and the epic's deliberate supersessions all verified as intentional. But the swap should not happen until two groups land. (1) The three CONFIRMED HIGH fixes, all small and precisely specified: add detail:{cart:cart} to both theme:cart:change dispatch sites (hairmnl-common.js:613, product-sticky-bar.liquid:256) plus the drawer re-render-at-open ordering fix; correct css-variables.liquid:575 to var(--FONT-WEIGHT-ACCENT-BOLD); re-point css-variables.liquid:536-539 at the defined heading tokens (honoring the 125% product-title size) with a PDP-title visual verify against P6. (2) The cutover-integrity mediums: delete mf-probe from the repo AND theme 141168312419; remove the bss-hide-variant.css 404 links (execute open ticket ujg6.57); resolve the group-footer 'subfooter' drift and run a full theme-pull parity diff against 141168312419 (this same diff settles the missing six FAQ PDP templates — port them or accept losing the hero-product FAQ/GEO content — and guards the cart-line-items upsell drift); add the sticky-bar theme-id gate-flip to the cutover checklist and drop the dead id 141655801955; add .auth/ to the root .gitignore, delete the stray storageState.json, and rotate the captured Shopify session. Strongly recommended in the same window because they are cheap, verified, and customer-visible under common conditions: the brand-collection '{-%' one-character fix, the line-537 search-stylesheet gate, the .on-sale / heading-cap / collection-grid__box chunk mirrors, the facet-AJAX trio (popstate, sticky offset, SRA unwrap), the popover toggle-close fallback, the brand-page mobile filter toggle (dead on all 31 brand templates), related.liquid eager-render for kr-product, buy-it-again guards before its precompute is ever scheduled, and container-type in the kt0 lint. The 21 lows are post-cutover cleanup and do not gate the swap.

## Critical (0)

_None._

## High (3) — all CONFIRMED by two independent verifiers

1. assets/hairmnl-common.js:613 and snippets/product-sticky-bar.liquid:256 dispatch theme:cart:change (and theme:cart:popdown) with no detail, so every theme.js listener that reads t.detail.cart (drawer controller theme.js:2696, header count/price theme.js:1711, free-shipping theme.js:1111) throws TypeError — the cart drawer opens EMPTY after a quick-buy add to an empty cart, opens stale (missing the just-added item) on the second and later adds per page session, header price and free-shipping progress never update, and 2-3 uncaught errors fire per add on the core ATC flow across every product-card surface plus the PDP sticky bar (server cart stays correct, so checkout completes); fix: dispatch with detail:{cart:cart} at both sites (the fresh /cart.js response is already in scope), add a defensive detail to the popdown dispatches, and — since the detail alone still leaves a stale first paint due to listener-registration order — also force the drawer re-render at open (dispatch theme:cart:reload first, or defer §7's theme:drawer:open via setTimeout 0). [CONFIRMED twice, double-verified; merges the core-js and purchase-path filings]

2. snippets/css-variables.liquid:575 aliases ---font-weight-accent-bold to --TYPE-WEIGHT-ACCENT, which is defined nowhere (the real token is --FONT-WEIGHT-ACCENT-BOLD at line 461), making 90+ fallback-less font-weight declarations (72 in theme.css, ~19 in theme-search.css, custom-theme.css:700, custom-theme-core.css:684, css-overrides.liquid:65 footer accordion titles and :1327 collection Filter/Sort toggles) invalid-at-computed-value-time — accent-bold text sitewide (uppercase-strong buttons, BUY IT NOW, footer titles, filter toggles, search results) silently loses its bold weight versus P6 live, an accidental typo from the P4 wave-A port (0cd30cf), not a design decision; fix: one line — change to var(--FONT-WEIGHT-ACCENT-BOLD), spot-check the listed surfaces on demo 141168312419, and normalize the .home__subtitle/tab-link chunk weight duplicates the broken alias currently masks. [CONFIRMED twice]

3. snippets/css-variables.liquid:536-539 alias all four --TYPE-*-PRODUCT-HEADING tokens to undefined --TYPE-*-HEADING names (the emitted tokens are --FONT-*-HEADING), so the PDP product title H1 (.product__title__wrapper .product__title in custom-theme-product/theme/home.css, all fallback-less) inherits body sans at body size instead of P6-live's SelfModern serif at ~21.4px on every product page — the 'may coincidentally resemble P6' hedge was refuted (settings_data.json has product_use_custom_heading_font:true + SelfModern + 125% size, which P6 honored); fix: emit the product-heading tokens from the still-shipping product_type_heading_* settings as P6 did (or at minimum alias stack/style/weight to --FONT-*-HEADING and adjust to 1.25 — plain --FONT-ADJUST-HEADING would silently shrink the title to ~17px), then visually verify the PDP H1 on demo 141168312419 against P6 live. [CONFIRMED twice]

## Medium (19)

1. assets/hairmnl-common.js:387 dispatches 'section:lazy-rendered' with zero listeners repo-wide, and theme.js's section registry initializes only once at DOMContentLoaded (its shopify:section:load re-init is designMode-gated), so the lazy-rendered related-products section NEVER populates on the storefront — templates/product.kr-product.json (verified as the only template with the section enabled; the other 9 carry disabled:true) shows a permanent ~400px 'You might also like' band with empty tabs and a dead recently-viewed rail; fix: eager-render sections/related.liquid via the 2i8b.35 '{%- if true -%}' pattern (note: dispatching shopify:section:load would NOT fix it — the storefront listener runs layout helpers, not section instantiation). [merges the core-js CONFIRMED and layout-config DOWNGRADED filings]

2. assets/hairmnl-collection.js:150-152 gates the facet-AJAX popstate reload on e.state.facetState and never replaceStates the initial history entry (state permanently null), so pressing Back from a filtered view — the most common back-navigation — leaves the filtered grid rendered under the unfiltered URL; fix: history.replaceState({facetState: location.pathname+location.search}, '', location.href) before the first pushState, or reload on any popstate once a swap has occurred.

3. assets/hairmnl-collection.js:24/36 writes the sticky filter-bar's dynamic top offset onto a .collection__nav node that the facet-AJAX innerHTML swap (line 112) destroys, so after any filter/sort interaction on ≤600px viewports the replacement bar falls back to css-overrides.liquid:840's top:0 and is covered by the z-5000 directional header exactly when users scroll up to reach it (an untriaged ujg6.16 × 2i8b.52 interaction, distinct from the closed 2i8b.81 repro); fix: publish the offset as a CSS custom property on document.documentElement and use top: var(--collection-nav-top, 0) so it survives DOM swaps.

4. assets/hairmnl-collection.js:112 assigns the raw Section Rendering API response — including its own #shopify-section-X wrapper — to sectionEl.innerHTML, so every filter/sort interaction nests another duplicate-id .shopify-section wrapper one level deeper with aria-busy on the wrong node (functionality survives, which is why single-filter QA passed); fix: unwrap via DOMParser and inject only doc.getElementById(sectionEl.id).innerHTML, matching theme.js's own loadForm pattern.

5. assets/hairmnl-common.js:812-813 resolves the Quick Buy popover via toggle.parentElement.querySelector while J24's portal (lines 738-741) parents the open popover to document.body, so re-tapping the open popover's own toggle icon is a completely swallowed no-op (stopImmediatePropagation runs before the null-check early return, making the toggle-close branch at 818 dead code) on every card surface, leaving aria-expanded='true' after the failed tap; fix: before the null return, fall back to the open portaled popover whose _qbHome === toggle.parentElement (tap-away/add-to-cart close paths still work, which caps this below high). [merges the core-js and cards-rails CONFIRMED filings]

6. sections/product.liquid:267 gates the sticky ATC bar on hardcoded theme ids including DELETED theme 141655801955, and no bd ticket or cutover-checklist step exists to flip the gate despite the file's own comment promising 'the P8 cutover flips show_sticky_purchase_bar to true' — the documented playbook publishes the demo directly (id preserved) so the bar ships if the plan is followed, but any plan deviation or post-cutover duplicate-then-publish silently and permanently removes the sticky bar with STKY uninstalled and no fallback; fix: drop the dead id now and add the gate-flip (plain true or a dependency-presence check) as an explicit step in bd a7av.7 / the cutover checklist. [absorbs the security-area dead-id filing]

7. assets/theme-home.css:25 (plus theme-collection/product/cart.css) re-emit h1/h2/h3 base + 480px-fluid rules WITHOUT the @media(min-width:1400px) caps theme-core.css carries, and the later-loading chunks win the specificity tie — headings including the PDP h1 grow unbounded on ≥1400px viewports (~+15% at 1920px, ~+33% at 2560px) and are inconsistent with search pages (a ujg6.42 Coverage-viewport artifact, same accidental class as shipped bug J28); fix: delete the duplicated heading rules from the four chunks (core already carries the full three-tier set) or append the matching cap blocks, and patch scripts/ujg6.42/wave-b-emit.py to emit all media tiers of a selector whenever any tier is emitted.

8. The .on-sale{color:var(--primary)} sale-price rule landed only in theme-home/collection/cart chunks, so discounted products emitted by snippets/product-grid-item.liquid:311 (also product-grid-item-branded.liquid:204 and sections/search.liquid:199) lose the sale color cue on PDP rec rails, native /search results, and the cart-drawer rail on chunkless templates (compare-at strikethrough survives, so price stays legible); fix: mirror the one-line rule in snippets/css-overrides.liquid beside the J28 .product__badge mirror — a no-op where chunks already carry it.

9. The .collection-slider .collection-grid__box display:grid layout rule exists only in assets/custom-theme-home.css:182, so ≤6-product Collection Slider instances on non-home templates render as full-width stacked cards instead of a centered grid — currently manifesting on collection.brand-dyson (1-product Corrale slider) and collection.cat.backbar-sizes (4 products), latent on other enabled instances that could drop below 7 products; fix: move the grid rule plus its max-width:1008px companions into custom-theme-core.css or css-overrides.liquid and verify on those two templates.

10. snippets/vertex-buy-it-again.liquid:59 filters only on p.available, missing the J11 S22/J16 render-time exclusions its sibling rail carries (salon-professional tags, the zero-price/'»'/'% off' title net that has leaked live before, already-in-cart skip) AND the J11 card unification (bespoke divs bypass vertex-rec-card and all J25-J29 rail styling) — on the surface most likely to contain freebies (purchase history); dormant only because the precompute Cloud Function is unscheduled (the metafield writer already exists — hairmnl-vertex main.py:578), so it activates silently the moment the job runs; fix: extract the shared rec_skip guard into a snippet both rails render and emit vertex-rec-card inside a .vrec-slot rail — before scheduling the precompute.

11. sections/brand-collection.liquid:125's #toggleFilters/#collectionFilters render only inside the lazy-hydrated content while shop.js.liquid:175/305 caches the (empty) jQuery selections and binds at ready with a .length guard, so the filter/nav toggle is deterministically a dead button below 769px on all 31 brand-collection templates — mobile users cannot open the brand sidebar at all (desktop is unaffected; the toggle never shows there); fix: eager-render the section or add a delegated document-level click handler for #toggleFilters (the already-dispatched 'section:lazy-rendered' event currently has no listeners).

12. layout/theme.liquid:537 still gates the theme-search/custom-theme-search stylesheet APPLICATION on template contains 'search' while only the sibling preload block at line 233 received yblz's page.handle=='search-results-page' guard, so the Searchanise page preloads both chunks at high priority but never applies them — the styling gap commit 6893167 promised to fix persists, plus two wasted preloads and 'preloaded but not used' warnings; fix: mirror the page.handle clause onto the elsif at line 537.

13. layout/theme.liquid:29-30 still load bss-hide-variant.css — deleted in epic commit 4303e70 with no replacement — so any clean push/cutover from repo state fires a sitewide 404 stylesheet request on every pageview (plus a blocking fetch on the noscript path); fix: delete lines 24-30 and execute the now-unblocked OPEN ticket bd ujg6.57 (its BSS-uninstall gate cleared 2026-06-10). [merges the layout-config and deletions-refs filings]

14. sections/brand-collection.liquid:18's malformed '{-%' delimiter (introduced by the d080833 consolidation; the P6 source is clean) turns the elsif into literal output, so a 3-column brand template with exactly 1 product — reachable via collection.branded-subcollection/branded/faq-kerastase — leaks the raw string '{-% elsif products_count == 2 -%}' into the rendered page and clobbers the centering offset; fix: change '{-%' to '{%-'.

15. templates/product.mf-probe.json + sections/mf-probe.liquid — the self-described 'Draft theme only — neutralize or remove after 2i8b.69 closes' metafield probe (ticket closed 2026-06-11) still ships at cutover: every product exposes a public 'PROBE OFF' page at ?view=mf-probe, 'mf-probe' appears in the admin product-template picker (mis-assignment replaces a product's buy box), and one boolean flip re-arms a public hmnl/custom metafield dump via the Section Rendering API; fix: git rm both files AND delete them from theme 141168312419 before the publish swap — a .shopifyignore is insufficient since they are already pushed. [absorbs the templates-json duplicate filing]

16. scripts/check-overlay-css.py:64's FORBIDDEN_PROPS omits container-type and the container shorthand even though the closeout's own kt0 regression class lists it and J29 introduced the repo's first use (snippets/css-overrides.liquid:~1592 — itself verified safe), so a future copy of the pattern onto a drawer/modal/header ancestor would pass CI silently despite two prior production incidents in this class; fix: add container-type/container (excluding normal/none) to the regex and update the script docstring plus the CLAUDE.md kt0 property list — the J29 selectors match no overlay pattern, so the lint stays green without new acks.

17. Root .gitignore lacks .auth/ while a real untracked .auth/storageState.json (5.3KB) sits at the P4 worktree root containing Shopify SSO identity-session and merchant-essential cookies valid into 2027 plus live facebook.com cookies (NOT safely stale — only the admin_session cookies expired), one bulk 'git add -A' or bd auto-export-git-add away from committing live session material to the private remote; fix: add .auth/ to the root .gitignore, delete the stray worktree file, and rotate/log out the captured Shopify session as a precaution.

18. sections/group-footer.json orders a 'subfooter' section whose sections/subfooter.liquid does not exist in the repo, and neither group JSON is rendered by layout/theme.liquid (static section tags only) — concrete evidence of repo↔Working-Demo drift (the p5-sync commit 94ad99d curated real merchant content into these files, implying subfooter.liquid exists on theme 141168312419 but was never pulled), so the repo cannot fully reproduce the theme going live and a full push could fail validation or silently drop the subfooter; fix: pull subfooter.liquid from the demo theme (or delete both unwired group JSONs) and run a full 'shopify theme pull --theme=141168312419' diff against HEAD before the publish swap. [critic-verified addition]

19. Verified during FAQ-template verification (preview fetch against theme 141168312419): the P8 Working Demo contains NONE of the six gwct.2 product.faq-*.json templates — hero PDPs (K18, Genesis serum, Olaplex 3, etc.) fall back to product.json — so the epic's FAQ/GEO content and FAQPage schema on the store's hero products silently do not ship at cutover; fix: port the six FAQ templates to the demo theme before the swap (blanking their stale upsell bindings first, per the low-severity drift note) or explicitly accept the content and schema loss.

## Low (21)

1. assets/hairmnl-common.js:810/571 keep STKY-rationale stopImmediatePropagation/stopPropagation on quick-buy toggle and vrec-add clicks after the app is uninstalled — permanent document-capture event suppression justified by stale 'preview only' comments; post-cutover cleanup: drop the stopImmediatePropagation (keep preventDefault + stopPropagation for the toggle) and reword both comments to the surviving card-link-navigation rationale.

2. sections/section-collection.liquid:90 emits its lazy-render URL without the theme.role != 'main' preview_theme_id guard its siblings received in 2i8b.48/2i8b.63, so Working-Demo homepage collection-row fetches resolve against the published P6 theme — preview-only confusion that self-heals at cutover; mirror the sibling pattern (correctly no-ops on live).

3. snippets/css-variables.liquid dropped P6's ---url-zoom/---url-zoom-2x definitions still consumed by assets/pswp.css:331-332, so both cursor declarations (including the ', zoom-in' list fallback) go invalid and the PDP lightbox shows a default arrow cursor on desktop (zoom itself still works); re-add the two url() definitions (confirming the SVG assets exist) or switch pswp.css to plain cursor: zoom-in.

4. snippets/css-overrides.liquid — J27's !important padding/letter-spacing (lines 1536-1544) beats J29's later non-important card-proportional clamp values (lines 1592-1605) on .vrec-card--standard only, so Vertex rail badges scale font-size but keep the fixed 5px/10px box, diverging a few px from the other five rail surfaces at the same width; move the clamp values into the J27 block with !important (or drop padding/letter-spacing from J27 now that J29 supplies them) and re-verify the Genesis PDP rail pill.

5. assets/theme-search.css:108-118 re-emits the early .popout__toggle,select icon rule after theme-core's later background-image:none reset, so sort/filter popouts on search templates (native /search and the Searchanise page) regain the select caret and 28px right padding, inconsistent with every other template; append the overriding .popout__toggle{padding:9px 16px;background-image:none} rule to the chunk or delete the re-emit.

6. assets/theme-product.css lacks .price-per-unit{opacity:.75} and the two grid__swatch alignment rules (placeholder margin reset, .text-center centering) that other template chunks carry, giving full-opacity unit prices and left-aligned swatch rows on PDP rail/search cards when those features render; cheapest fix: add all three rules to the J28 css-overrides mirror block, closing the product-grid-item chunk-gap family template-independently.

7. snippets/product-grid-item-branded.liquid:~171's forloop-based 3-tier eager-loading is dead code on the P8 shipping path — brand-collection.liquid's {% render %} callsites pass no index, so forloop is nil and every card silently falls to loading="lazy" — with NO customer-facing LCP impact (the only consumers are below-fold, IntersectionObserver-lazy-hydrated rails, and the pattern never worked on this path: the render conversion predates the 987.1 port); pass card_index from the callsites and guard the conditional, or simplify to unconditional lazy, and annotate bd 987.1 that its fix does not carry to P8.

8. snippets/products-recommendation.liquid:4 keeps data-grid-large="4" while the recently-viewed rail is 6-across, reproducing on product.backbar-product/promo-product/faq-genesis-serum/faq-purifying (which place BOTH rails) exactly the two-mismatched-rails density symptom 2i8b.55 fixed on the main PDP; change it to 6 (or read the section's limit setting) and verify one faq/promo template.

9. snippets/product-sticky-bar.liquid:259-261 and hairmnl-common.js:618-620 only console.warn when /cart/add.js rejects (e.g. 422 inventory race), so the shopper's tap silently does nothing and the button reads as dead (the primary PDP form, by contrast, renders into [data-add-action-errors]); surface a transient error state on the CTA or toast the response's description field — cutover-optional.

10. sections/cart.liquid:70-77 renders the Vertex rail inside the cart <form> under a 'formless cards' contract that product-grid-item would break if swatches_enable were ever turned on (it emits a per-card <form>; currently false in settings_data.json:57, so the contract holds at HEAD); pass hide_swatches through vertex-rec-card for cart placements or add one line to the a7av.10 comment documenting the dependency.

11. sections/brand-collection.liquid:209's size==0 guard can never be false (joining blank settings then splitting yields ['','']), so 28 of 31 brand templates render orphan 'Featured Collections'/'Discover <brand>' headings over empty grids — likely parity-with-live since the guard bug is inherited from the P6 source; guard on actual collections[handle] presence and spot-check 2-3 brand pages (krsymbiose, blondabsolu, elixir) against P6 live to confirm parity vs settings loss.

12. layout/theme.liquid:170's satcb.azureedge.net dns-prefetch (STKY CDN) and the lines 1901-1919 LimeSpot container-mover MutationObserver (15s per page) are decommissioned-vendor leftovers NOT covered by bd a7av.7's cleanup list; append the dns-prefetch + ordering-comment removal to a7av.7 and fold the LimeSpot mover removal into the LimeSpot uninstall ticket.

13. config/settings_data.json:714 retains an inert current.sections entry for deleted section type bss-b2b-wholesaler-form-1959 — the only entry whose type file is missing (content_for_index is empty, no section tag references it); remove the three-line stub before the publish swap or accept as admin-managed noise.

14. scripts/check-overlay-css.py:41's OVERLAY_PATTERNS was never extended with the four fixed-position overlays the epic itself created (.quick-buy__popover/[data-qb-popover], .vrec-product-sticky-bar, .sticky-cart-bubble, .vrec-fly-clone) despite CLAUDE.md's explicit requirement and the J24 popover having already demonstrated the trap class in-epic; add the selector patterns (false positives are acceptable via the existing kt0-OK ack path). [merges the kt0-sweep and scripts-ci filings]

15. assets/vertex-recommendations.css:513-595 ships ~80 lines of dead team-review notes-panel CSS (.vrec-slot--preview/.vrec-slot__notes*, 11 selectors) whose markup commit 86d0d07 removed entirely, including a now-false 'NEVER renders on live' comment; delete the block.

16. docs/preview-review-deck-r5.build.js:12 and r6.build.js:12 hardcode deleted preview theme 141655801955, so re-running either generator produces review-deck links that silently fall back to LIVE P6 — the exact which-theme confusion that already burned the 2i8b.81 diagnosis; point NF at 141168312419 (the R7 generator already does) or mark both scripts DEFUNCT.

17. templates/product.faq-{k18,genesis-serum,oi-milk,energizing,purifying,olaplex3}.json:68/84 carry the Liquid-in-static-settings upsell bindings that ece553c blanked in product.json — verified as parity-with-live rather than a re-armed defect (the PDP 'Buy it with' renders on P6 today, the upsell_list branch is commented out in sections/product.liquid, and the P8 theme lacks these templates entirely), but blank the two settings to match product.json before any port of the FAQ templates to P8 so the bindings don't fail push validation or ride along.

18. templates/agents.md.liquid:94 still instructs agents to GET the robots-disallowed /search endpoint after uyq6.5 (2fe11b8) rewrote only llms.txt.liquid's Search line, breaking the files' self-declared 'edit both together' body-identity invariant and leaving the two agent contracts contradictory; copy llms.txt.liquid:93's UCP search_catalog line verbatim into agents.md.liquid.

19. snippets/cart-line-items.liquid:153-178 at main HEAD still renders the 'BUY IT WITH' upsell that closed ticket bd 2i8b.40 removed out-of-tree — verified INTACT (commented out) on the actual cutover artifact theme 141168312419 and ledgered in bd 2i8b.38, so nothing wrong ships at the publish swap, but any main→demo push of this file (e.g. re-running the checklist's Phase A full sync) silently resurrects the upsell; add a pre-cutover git↔theme parity diff of the 2i8b.38-ledgered files to the checklist (noting reverse drift: the theme copy lacks hod's cart img width/height attributes).

20. snippets/media.liquid:79-85 hardcodes loading="eager" fetchpriority="high" on EVERY external_video lite-youtube poster while the sibling image branch (lines 45-46) correctly gates on is_first, so a PDP with a YouTube video in a non-lead gallery slide fires a high-priority i.ytimg.com fetch that competes with the true LCP image; gate both attributes on is_first exactly as the image branch does. [critic-verified addition]

21. assets/hairmnl-common.js §7 (drawer-on-ATC v2, 23e36a5) dispatches theme:drawer:open from a change listener that registers BEFORE theme.js's drawer controller, so even the theme's NATIVE detail-carrying ATC flow paints a stale drawer on the second consecutive add (§7's own 'guarantees stale=true' comment is wrong; heals on close/reopen) — a distinct discovery from the high-severity dispatch finding; fix alongside it by deferring §7's drawer:open (setTimeout 0) or dispatching theme:cart:reload first, and file as its own ticket.

## Passed

60 explicit checks passed at HEAD (main, 32eb775) across the six audit areas, independently spot-re-verified by the synthesizer on the load-bearing items. Highlights: the purchase path's wiring is sound (sticky-bar↔product-form contract, skip-to-checkout ships OFF by default, templates/cart.json valid with no double Vertex render, no nested forms at current settings, app-embed inventory clean of BSS/Appikon/LimeSpot with the STKY embed already ticketed in a7av.7); kt0 containment is clean (scripts/check-overlay-css.py exit 0 over 367 files; J29's container-type use, the flyToCart clone, sticky-cart-bubble, and the popover body-portal all verified trap-free against the two-incident regression class); every suspected supersession checked out as DELIBERATE, not regression (J27 replacing J24/J25 close-on-scroll, J26 removing the title clamp, the Vertex home-embed move to index.json, J10 S11's collection sticky-bar removal); CSS cascade premises hold (.hairmnl-theme scoping class present, stylesheet order as designed, J28 badge mirror complete on all templates, critical-css safety-nets the chunkless templates); §7's popdown-suppression ordering is correct for the stock ATC flow; preview_theme_id gating on shipping lazy-URLs correctly no-ops at cutover; no debug code, console.log, or hardcoded theme ids leak into shipped CSS/JS beyond the triaged product.liquid gate; locales and section-recent-products untouched; the Reamaze defer-guard chain is intact. Excluded per the rules: 1 REFUTED finding (quick-buy popover hidden behind the header mega-menu — unreachable at HEAD: the header's product blocks are disabled in settings_data.json and group-header.json is dead config; an optional z-index hardening note was retained) and 0 VERIFY_FAILED. The critic's sweep verified the previously uncovered surfaces (assets/hairmnl-product.js benign; the perf-wave section diffs l76r/ujg6.5/19/41/za7u.1/.6/y79d/fwar/oo57 all deliberate and labeled; .githooks/pre-push non-shipping) and contributed two additional verified findings, folded into the buckets. Residual unexercised lenses for the operator: full repo↔Working-Demo parity (two concrete drift instances WERE confirmed — group-footer subfooter and the missing FAQ templates — so run a full 'shopify theme pull --theme=141168312419' diff before the swap), keyboard/focus accessibility of the new interactive components, hardcoded-English strings in the new UI (acceptable for a single-locale store but undocumented), and paginated collection views.

## Completeness critic

**Verdict:** Coverage by the 12 auditors was strong where it mattered most — purchase path, cart events, kt0 containment, CSS chunk split, deletions, security surfaces, and ship hygiene were audited redundantly and at HEAD. The real coverage holes clustered in two places: (1) the standalone perf-wave section diffs (footer, menu-buttons, article, section-double/product/slideshow/static-banner, media.liquid) which no coverage report mentioned — I skimmed all of them and they are deliberate, well-labeled waves (l76r, ujg6.5/19/41, za7u.1/.6, y79d, fwar, oo57) with one incomplete fix (eager video poster, low); and (2) the two new section-group JSON files, which nobody validated and which yielded the one substantive new finding: group-footer.json references a 'subfooter' section absent from the repo while both group files are unreferenced by the layout — a repo-vs-Working-Demo drift signal that should be resolved before the cutover publish swap. hairmnl-product.js (the one explicitly declared gap) verified benign. No critical or high finding was missed; the epic remains shippable once the subfooter drift question is answered against theme 141168312419.

**Uncovered areas:** sections/group-header.json and sections/group-footer.json (both ADDED in-range) — no auditor's coverage report mentions them; templates-json validated only templates/*.json. I audited them myself: see additional finding 1 (nonexistent 'subfooter' section type).; assets/hairmnl-product.js (NEW) — core-js explicitly declared it out of scope and no one else read it. I read it in full (37 lines): a benign DOMContentLoaded tab-switcher (.tab-button/.tabs) ported from P6, correctly gated in layout/theme.liquid:920-922 to 'template contains product', markup exists in snippets/product-tabs.liquid and three sections. No issues.; The 'perf wave' section diffs no auditor covered: sections/footer.liquid (l76r rich-text img lazy-load), sections/menu-buttons.liquid (ujg6.5 inline CLS CSS), sections/article.liquid (fwar is_first + oo57 LimeSpot CSS hide), sections/section-double.liquid + section-static-banner.liquid (ujg6.41 YouTube facade), sections/section-product.liquid (y79d featured-media-first dedup, za7u.6 description facade), sections/section-slideshow.liquid (ujg6.19 eager first slide), snippets/media.liquid (za7u.1 lite-youtube poster), snippets/custom-fonts.liquid, snippets/article-grid-item.liquid. I skimmed every one of these diffs: all are labeled, deliberate waves; one incomplete-fix found in media.liquid (additional finding 2).; assets/theme.dev.js — only the RecentProducts region was read (cards-rails); its 26-line diff and its parity with the shipped theme.js were never checked. It is not loaded by the theme, so risk is drift-only.; assets/hairmnl-custom.js full body — security read lines 150-208 only; core-js confirmed it is dormant (no load references) pending deletion under bd 2i8b.20. Residual risk is nil at runtime but it is 399 lines of shipped-but-dead asset weight.; sections/header.liquid full body — covered only at grep level (data-header-cart-count markup) despite being modified in-range.; .githooks/pre-push (NEW) — unmentioned by any auditor. I read it: benign dev tooling wrapping scripts/check-snippet-wiring.py; does not ship to the theme.

**Lenses not run:** Repo-vs-Working-Demo drift verification: every auditor was static/repo-only. The group-footer.json 'subfooter' gap is concrete evidence the repo may not fully reproduce theme 141168312419 (the p5-sync commit 94ad99d pulled 'missing snippets from live', implying prior drift). A pre-cutover 'shopify theme pull --theme=141168312419' diff against HEAD was never run by anyone.; Systematic accessibility of the new interactive components: quick-buy popover, product-sticky-bar, sticky-cart-bubble, notify-when-available-modal all have aria-* attributes (I grepped: 6/9/2/6 occurrences respectively) but nobody audited keyboard operability, focus trap in the notify modal, or popover dismissal semantics.; i18n/hardcoded strings: locales/ has ZERO diff in the epic (I confirmed), so every user-facing string in the new components (sticky bar, quick-buy, notify modal, facade 'Play video' labels) is hardcoded English rather than t:-filtered. Acceptable for a single-locale store but no one made that call explicitly.; Cumulative Liquid render cost of the epic: multiple new per-render string pipelines (footer l76r double-replace on three rich-text blocks, za7u.6 capture+render of youtube-facade-content over full product descriptions, article-content-with-img-dims chaining) — no one assessed server-side render cost or Section Rendering API impact.; Pagination / page-2+ collection views: collection auditors covered filters/sorting/grid statically but no lens exercised paginated output of the rewritten brand-collection/collection sections.; theme.js vs theme.dev.js source/compiled parity — if theme.dev.js is the build source, unported edits in either direction silently diverge future builds.

## Filed tickets

| bd | Sev | Title |
|---|---|---|
| 2i8b.83 | HIGH | theme:cart:change dispatched without detail |
| 2i8b.84 | HIGH | accent-bold weight alias typo (sitewide) |
| 2i8b.85 | HIGH | product-heading token aliases undefined (PDP H1 font) |
| 2i8b.86 | MED | pre-publish theme-vs-repo parity (subfooter, FAQ templates, 2i8b.38 ledger) |
| 2i8b.87 | MED | delete mf-probe from repo + theme |
| 2i8b.88 | MED | .auth/ session-material hygiene + rotation |
| 2i8b.89 | MED | facet-AJAX trio (popstate / sticky offset / nested wrappers) |
| 2i8b.90 | MED | CSS chunk-gap family (.on-sale, heading caps, grid rule + 3 lows) |
| 2i8b.91 | MED | quick-buy popover re-tap no-op |
| 2i8b.92 | MED | brand-collection {-% leak + dead mobile filter toggle |
| 2i8b.93 | MED | related.liquid never populates (kr-product) |
| 2i8b.94 | MED | search stylesheet apply-gate misses search-results-page |
| 2i8b.95 | MED | vertex-buy-it-again guards before precompute |
| 2i8b.96 | MED | kt0 lint: container-type + epic overlays |
| 2i8b.97 | LOW | post-cutover cleanup umbrella (21 items) |

Plus checklist appends on a7av.7 (sticky-bar gate flip, satcb dns-prefetch, LimeSpot mutation-observer removal, settings_data bss stub) and ujg6.57 (bss-hide-variant 404 confirmation).
