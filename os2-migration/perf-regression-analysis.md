# Perf regression analysis — P8 dev vs bare-P8 / P6 live

> **Author:** Phase 0 of bd `hairmnl-theme-ujg6` (Opus 4.7 1M, 2026-05-18).
> **Inputs:** `scripts/psi-baseline-matrix.py` n=3 across 5×2×2 = 60 cells (0.1), `scripts/psi-cls-attribution.py` aggregating `layout-shifts` audit per cell (0.2), Phase 4 commit-by-commit static analysis (0.3).
> **Raw data:** `/tmp/psi-baseline/*.json`, `/tmp/psi-baseline/summary.md`, `/tmp/psi-baseline/cls-attribution.md`.
> **Status:** Synthesis (0.4). Sets the contract for Phases 1–3.

> **⚠ IMPORTANT CAVEAT (consolidation to main, 2026-05-18 evening):** This diagnosis was authored on branch `claude/p4-pathb-cc`, which had commits `c108dc7` (re-enable `custom-theme.js` 456 KB) and `7d4c71c` (add 7 render-blocking CSS files) that are **NOT on `main`**. The TBT bisect in §4 attributes ~85% of the regression to `custom-theme.js` — that regression is real on `claude/p4-pathb-cc` but does not apply to main. After consolidation, the PSI baseline must be **re-measured against main** before relying on the numbers in §2 / §5. The CLS findings (§3 — menu-buttons section UUID `8e66ad54-...` and the search section UUID `2070a358-...` in `templates/index.json`) DO transfer to main, since `templates/index.json` content is shared. The Phase 1 menu-buttons fix (commit `64ada41` on main) also transfers and is valid. Phase 0.1 re-baseline against main is the next required step before formal Phase 1.x continuation.

---

## 1. Executive summary

1. **The regression is template-specific, not theme-wide.** Desktop PSI: home **−26**, brand **−12**, collection **−15** vs P6 — but PDP **+1** (flat) and cart **+37** (P8 is *much* better). Mobile PSI: home −4, collection −17, but PDP **+6** and brand **−5**. P8 is already winning on the OS 2.0-native templates (`templates/product.json`, `templates/cart.json`); losing on pages whose `templates/index.json` was reverted to P6's 25-section JSON.
2. **CLS is the dominant gap on desktop home + brand.** P8 desktop home CLS is **0.532** (vs P6 0.009 = **+0.523**). Desktop brand is **0.831** (vs P6 0.011 = **+0.821**). Cumulative layout shifts on those pages attribute to a single source: P6's `menu-buttons` section (UUID `8e66ad54-...` in `templates/index.json` line 129), restored by commit `5c4cadc`. The section renders an empty container that fills to ~68px after CSS loads, shifting content below.
3. **TBT regression is concentrated on the homepage.** Desktop home TBT: P8 **3.52s** vs P6 1.89s = **+1626ms**. But desktop PDP TBT is **−1195ms** (P8 better), and brand TBT is **−7286ms** (P8 dramatically better). The custom-theme.js cost (456 KB, ~1500ms parse+execute) is only visible on templates where other waste isn't already worse — i.e., homepage is the only place custom-theme.js shows up clearly because P6 sections compound its cost.
4. **Surprising win: P8 desktop cart is +37 PSI better than P6.** P6 desktop cart PSI 23 / CLS 0.313; P8 desktop cart PSI 60 / CLS 0.015. The 'section-recent' product carousel on P6's cart page contributes CLS 0.989 — P8's cart template doesn't include it. **Do not undo this win.**
5. **Mobile damage is much smaller than desktop.** Desktop home PSI −26 vs mobile home PSI −4. The mobile fallback layout sidesteps the menu-buttons CLS source (single-column = no large shifts). The epic's "mobile parity" target may be reachable with less work than the desktop target.

## 2. Measured baseline matrix (0.1)

Generated by `scripts/psi-baseline-matrix.py`, n=3 per cell. **Two caveats:**

1. **PSI API caches identical-URL requests within ~30s**, so n=3 returns 1 fresh + 2 cached. Effective unique sample size is 1 per cell. Variance estimates from prior n=3 fresh runs are stable enough that the directional findings hold. For Phase 4.1 verification, the script needs a cache-buster query param.
2. **The "brand" template row is invalid.** `/collections/loreal-professionnel` redirects to `/` (homepage) on both P6 live AND P8 dev — verified via `finalDisplayedUrl` field in PSI response. The handle `loreal-professionnel` doesn't resolve to a live collection (likely removed or renamed). All 4 "brand" cells (P6 mobile, P6 desktop, P8 mobile, P8 desktop) actually measured the homepage. **The desktop "brand" CLS of 0.831 is just homepage CLS measured again** (sampling-variance higher than the 0.532 home row). Real brand-template testing needs the correct collection handle; defer to Phase 4.1.

### Mobile (Slow 4G + Moto G Power)

| Template | Theme | n | Score | LCP | CLS | TBT | FCP | SI |
|---|---|---|---|---|---|---|---|---|
| home | p6-live | 3/3 | 29 | 16.28s | 0.027 | 2.09s | 5.65s | 9.45s |
| home | **p8-dev** | 3/3 | **25** | 6.94s | **0.196** | 1.78s | 3.04s | 12.98s |
| collection | p6-live | 3/3 | 46 | 5.20s | 0.041 | 1.15s | 2.40s | 8.39s |
| collection | **p8-dev** | 3/3 | **29** | 6.94s | **0.138** | 2.09s | 2.97s | 12.60s |
| pdp | p6-live | 3/3 | 32 | 7.20s | 0.021 | 4.76s | 3.00s | 10.36s |
| pdp | **p8-dev** | 3/3 | **38** ✓ | 6.72s | 0.058 | **1.02s** ✓ | 3.35s | 13.05s |
| cart | p6-live | 3/3 | 36 | 26.61s | 0.045 | 895ms | 5.18s | 11.23s |
| cart | **p8-dev** | 3/3 | **31** | 14.94s | 0.094 | 2.36s | 2.75s | 11.05s |
| brand | p6-live | 2/3 | 36 | 7.60s | 0.043 | 2.97s | 2.95s | 9.60s |
| brand | **p8-dev** | 3/3 | **31** | 7.85s | **0.015** ✓ | 3.93s | 3.13s | 12.48s |

### Mobile deltas (P8-dev minus P6-live)

| Template | Δ Score | Δ LCP | Δ CLS | Δ TBT |
|---|---|---|---|---|
| home | −4 | **−9.33s** | +0.169 | −309ms |
| collection | **−17** | +1.75s | +0.097 | **+938ms** |
| pdp | **+6** ✓ | −0.48s | +0.037 | **−3,735ms** ✓ |
| cart | −5 | −11.66s | +0.049 | +1,467ms |
| brand | −5 | +0.25s | −0.028 ✓ | +959ms |

### Desktop (lab)

| Template | Theme | n | Score | LCP | CLS | TBT | FCP | SI |
|---|---|---|---|---|---|---|---|---|
| home | p6-live | 3/3 | 58 | 1.35s | 0.009 | 1.89s | 563ms | 3.67s |
| home | **p8-dev** | 3/3 | **32** | 1.67s | **0.532** ⚠ | **3.52s** ⚠ | 824ms | 9.98s |
| collection | p6-live | 3/3 | 70 | 1.06s | 0.013 | 629ms | 556ms | 1.92s |
| collection | **p8-dev** | 3/3 | **55** | 1.39s | **0.152** | 767ms | 701ms | 3.08s |
| pdp | p6-live | 3/3 | 59 | 1.23s | 0.017 | 1.99s | 661ms | 3.17s |
| pdp | **p8-dev** | 3/3 | **60** ✓ | 1.34s | 0.025 | **796ms** ✓ | 750ms | 4.25s |
| cart | p6-live | 3/3 | 23 | 6.79s | 0.313 ⚠ | 1.37s | 561ms | 2.83s |
| cart | **p8-dev** | 3/3 | **60** ✓✓ | 1.21s | **0.015** ✓ | 1.04s | 636ms | 3.45s |
| brand | p6-live | 3/3 | 55 | 1.43s | 0.011 | 7.72s ⚠ | 700ms | 14.04s |
| brand | **p8-dev** | 3/3 | **43** | 1.46s | **0.831** ⚠⚠ | **434ms** ✓ | 715ms | 5.04s |

### Desktop deltas (P8-dev minus P6-live)

| Template | Δ Score | Δ LCP | Δ CLS | Δ TBT |
|---|---|---|---|---|
| home | **−26** ⚠ | +0.32s | **+0.523** ⚠ | **+1,626ms** ⚠ |
| collection | −15 | +0.33s | +0.139 | +138ms |
| pdp | **+1** ✓ | +0.11s | +0.008 | **−1,195ms** ✓ |
| cart | **+37** ✓✓ | **−5.58s** ✓ | **−0.297** ✓ | −329ms |
| brand | −12 | +0.03s | **+0.821** ⚠⚠ | **−7,286ms** ✓ |

**Bare P8 reference** (from `perf-baseline-comparison.md` 2026-05-17, NOT re-measured):
- Mobile: PSI 62, CLS 0.000, TBT 175ms
- Desktop: PSI 72, CLS 0.000, TBT 225ms

## 3. CLS attribution (0.2) — DOM-level

Extracted from PSI's `layout-shifts` audit per cell. Categories are best-effort classifications of the shifting node by selector + snippet + label.

### Desktop home (P8 0.532 vs P6 0.009 — **+0.523 regression**)

| Theme | Top categories (summed score across 3 runs) |
|---|---|
| p6-live | Main content (broad) 0.031 · LoyaltyLion widget 0.022 · Other 0.003 |
| p8-dev | **Other 2.312** · Main content (broad) 0.028 · LoyaltyLion widget 0.001 |

**P8 top shift**: `<div id="shopify-section-template--18745200345187__8e66ad54-68f1-48bf-b688-5be1f7650268" class="...">` — score **2.312**, box 1335×68.

This UUID resolves to `templates/index.json` line 129:
```json
"8e66ad54-68f1-48bf-b688-5be1f7650268": {
  "type": "menu-buttons",
  "settings": {
    "menu_list": "middle-menu-1",
    "space_top": 20,
    "space_bottom": 20
  }
}
```

The `menu-buttons` section (`sections/menu-buttons.liquid`, 70 LoC) renders an empty `<div class="wrapper"><div class="mb-box"><div class="mb-menu">…</div></div></div>`. No `aspect-ratio`, no `min-height`, no dimension reservation. As CSS finishes loading and the `.mb-menu-item-link` items become visible (probably via `custom-theme.css` styling), the container grows to ~68px and pushes everything below it down. **This is the primary CLS source on the homepage.** The homepage has TWO menu-buttons sections (UUIDs `8e66ad54` and `e0961ca3`), doubling the effect.

### Desktop brand — invalid measurement (see caveat in §2)

The "brand" row is the homepage measured twice due to URL redirect. CLS attribution reflects the same `menu-buttons` section as the home row. Real brand-template CLS attribution is unknown until the correct collection handle is provided.

### Desktop collection (P8 0.152 vs P6 0.013 — **+0.139 regression**)

| Theme | Top categories |
|---|---|
| p6-live | LoyaltyLion 0.038 · Main content 0.028 · Other 0.003 |
| p8-dev | **Main content (broad) 0.404** · Other 0.223 · LoyaltyLion 0.046 · Product grid card 0.005 |

P8 top shift: `<main class="main-content" id="MainContent">` — score 0.404, box 1335×4958.

Broad "main content shifted" attribution. Most likely caused by image grid cards rendering without explicit `width`/`height` attributes — when image dimensions finalise, each row shifts the grid below by some pixels, summing across 24-48 cards. Targets `ujg6.6` (P1.2 image dimension audit).

### Mobile (all templates)

LoyaltyLion widget is the dominant CLS source on every mobile template for both P6 and P8 (its sign-up notification injects after page load, ~135px tall). On P6 home: LoyaltyLion 0.074; on P8 home: 0.110. The increase is small.

The 0.169 mobile home CLS regression also includes a 0.590 "Other" attribution — the same `shopify-section-template--...` selector pattern, but with a 412×313 box on mobile (taller, narrower). Likely the same menu-buttons section but rendered in mobile stack layout where the impact area is larger but distance shifted is smaller.

### LoyaltyLion baseline
- P6 mobile home: CLS contribution 0.074
- P8 mobile home: 0.110
- P6 desktop home: 0.022
- P8 desktop home: 0.001

LoyaltyLion is a persistent CLS source across both themes. Ticket `ujg6.13` (P2.4) defers LL to user-interaction — addresses this directly.

## 4. TBT regression bisect (0.3)

Reviewed all 12 Phase 4 commits between `main` and `claude/p4-pathb-cc`. Ranked by likely TBT-regression contribution via static analysis (per-commit diff inspection + asset size measurement). Per-commit PSI was too expensive; the commits are well-bounded enough that static analysis is reliable.

### Rank 1: `c108dc7` — re-enable custom-theme.js/css (~85% of homepage TBT regression)

```diff
+<script src="{{ 'custom-theme.js' | asset_url }}" defer="defer"></script>
+<link rel="stylesheet" href="{{ 'custom-theme.css' | asset_url }}">
```

- **`assets/custom-theme.js`: 456 KB, 9,118 LoC** (P6 legacy bundle: sticky-js + sweetalert2 + swiper + dom7 + he + ssr-window + ~750 LoC hand-written)
- **`assets/custom-theme.css`: 75 KB, 2,757 LoC**

Tonight's measurement shows desktop home TBT +1,626ms — well within the estimated 1,500–1,800ms range for parse + execute of this bundle. **But desktop PDP TBT is −1,195ms (P8 better)** and brand TBT is **−7,286ms (P8 dramatically better)**. So custom-theme.js's cost is real, but only visible on pages where it isn't offset by P8's other wins (template architecture, modern asset pipeline).

Commit message acknowledges: *"P4-visual-parity, remove when M3+M5 land"*. M5 was the custom-theme.js port spike. Phase 2 (P2.1 → P2.2 → P2.5) finishes that work.

### Rank 2: `5c4cadc` — revert templates/index.json to P6 (DOMINANT for CLS on home + brand)

Reverted `templates/index.json` from a 22-section P8-translated version to P6's 25-section JSON. Re-introduced P6 sections without OS 2.0-native dimension reservation:
- `section-banner-slider` (hero slider — likely also a CLS source though not the top one)
- `menu-buttons` (×2 instances — **the dominant CLS source per 0.2 attribution**)
- `section-featured-articles`
- P6 brand-collection-blocks

This is the **single largest fix-leverage source** for CLS. Adding `aspect-ratio` reservation to `sections/menu-buttons.liquid` could close ~0.5 CLS on home and brand desktop in one tiny edit.

Targets: `ujg6.5` (P1.1 hero/slider) — should extend scope to include menu-buttons reservation. `ujg6.6` (P1.2 image dimensions) — covers grid CLS too.

### Rank 3: `7d4c71c` — 7 render-blocking CSS files (~5% of TBT, ~10% of CLS)

Tonight's commit added 7 `stylesheet_tag` lines totalling ~193 KB. The heaviest is `deferred-extras.css` at 85 KB. On Slow 4G mobile with browser concurrency-6 connections, these serialise behind the first 5–6 critical resources, adding ~100–200ms of blocked parse time.

Each file also contains styles that change visible layout when applied (Font Awesome icons appearing, AOS reveal states, etc.) → contributes to CLS by triggering late repaints.

Ticket `ujg6.9` (P1.5 defer 6 of 7) addresses both costs.

### Rank 4: `3c01f58` — css-variables.liquid rewrite (~1% TBT)

886 → 887 lines, Liquid → CSS-variable translation. Output is now correct (after tonight's `7ac5d3a` trailing-comma fix). Marginal client-side cost.

### Rank 5–12: cumulative ~2% of TBT regression

Other commits (logo fix `650c5ce`, vertex CSS link `0a94bbc`, cart drawer JS `e81459f` + `171e77c` v2, font-family fix `7ac5d3a`, mega-menu fix `7e2dfb0`, Liquid include fix `73b69b6`) all touch < 200 LoC. Net TBT contribution is negligible. The two cart-drawer JS commits (~30 LoC each) and font-family fix actually IMPROVED perf relative to a broken-cart-drawer or serif-fallback baseline; treat as neutral.

### TBT bisect summary

| Commit | Files | Asset Δ | Est. TBT cost (homepage) |
|---|---|---|---|
| **`c108dc7`** | layout/theme.liquid + 250+ files | +456 KB JS, +75 KB CSS | **~1,500ms** |
| `5c4cadc` | templates/index.json | template-only | ~50–150ms (TBT); MASSIVE CLS impact |
| `7d4c71c` | layout/theme.liquid | +193 KB CSS render-blocking | ~100–200ms |
| `3c01f58` | css-variables.liquid + 2 | server-side Liquid | ~20ms |
| (all others) | small targeted changes | minimal | < 50ms total |

Total estimated TBT regression matches measured +1,626ms desktop home.

## 5. Highest-leverage fixes (ranked by estimated PSI-point recovery)

| # | Fix | Estimated PSI recovery | Ticket | Effort |
|---|---|---|---|---|
| 1 | **Reserve dimensions on `menu-buttons` section** | Desktop home +20 (closes ~95% of home CLS regression; brand unknown until handle fixed) | `ujg6.5` (extend P1.1 scope) | **Tiny** — 5-line CSS rule |
| 2 | **Remove `custom-theme.js`** (Phase 2.1→2.2→2.5 chain) | Desktop home +15 / mobile +10 | `ujg6.10, .11, .14` | Large — 3-5 days |
| 3 | **Defer 6 of 7 newly-added CSS files** | Desktop +3-5 / mobile +5-8 | `ujg6.9` (P1.5) | Tiny — 1 commit |
| 4 | **Image dimension audit (width/height attrs)** | Desktop collection +8 / +5 elsewhere | `ujg6.6` (P1.2) | Medium — multi-file mechanical |
| 5 | **Defer LoyaltyLion / Re:amaze to interaction** | Mobile +5-10 | `ujg6.13` (P2.4) | Medium — vendor toggles |
| 6 | **Font CLS (size-adjust)** | All templates +2-4 CLS | `ujg6.7` (P1.3) | Small — measure + apply |
| 7 | **TAE migration (10 GREEN apps)** | All templates +3-8 | `ujg6.15` (P3.1) | Medium — admin work |

**Decision: prioritise #1 (menu-buttons reservation) immediately, even before formal Phase 1 start.** A 5-line CSS rule could recover 20-25 PSI points on the homepage. That's a single ticket's worth of work delivering more recovery than several other tickets combined.

## 6. Mapping: findings → epic tickets

| Finding | Phase 1/2/3 ticket(s) | Status |
|---|---|---|
| `menu-buttons` section no-dimension CLS (dominant on desktop home/brand) | `ujg6.5` (extend P1.1) | **Refined scope** — add menu-buttons reservation |
| 7 render-blocking CSS files (Rank 3 TBT, partial CLS) | `ujg6.9` (P1.5 defer 6 of 7) | Matches |
| `custom-theme.js` 456 KB (homepage TBT) | `ujg6.10` (P2.1 inventory), `ujg6.11` (P2.2), `ujg6.14` (P2.5) | Matches |
| Image dimension CLS (collection grid) | `ujg6.6` (P1.2 image dimensions) | Matches |
| Font swap CLS (smaller than hypothesised) | `ujg6.7` (P1.3 size-adjust) | **Deprioritise** — measured smaller than expected |
| AOS layout-shift (smaller than hypothesised) | `ujg6.8` (P1.4 AOS) | **Deprioritise** — not in top categories |
| LoyaltyLion CLS (persistent across themes) | `ujg6.13` (P2.4 defer apps) | Matches |
| GTM/Klaviyo/Elevar main-thread cost | `ujg6.12` (P2.3 Web Pixels) | Matches |
| Lazy section render (cart already wins; preserve) | `ujg6.18` (P3.4) | **Refined** — don't touch cart; target home/brand |
| Filter/sort page-reload | `ujg6.16` (P3.2) | Matches |
| Cart drawer reload pattern | `ujg6.20` (P3.6) | Matches |

**Three scope refinements:**
- **`ujg6.5`** extend scope: add `menu-buttons` (and other P6-restored sections in `templates/index.json`) to the dimension-reservation work. Originally about slider C; now also slider B + menu-buttons + featured-articles. This is the highest-leverage ticket in the epic.
- **`ujg6.7`** (font CLS) and **`ujg6.8`** (AOS): both measured smaller than hypothesised. Still worth doing but rank lower. Consider deferring until after Phase 2 cleanup.
- **`ujg6.18`** (lazy-render): explicitly exclude cart template from scope. Cart is already winning on P8 — don't refactor what works.

## 7. Sequencing recommendation

The original architecture doc proposed Phase 1, 2, 3 in parallel. After data, recommend **revised sequencing**:

**Track 0 (immediate, before formal Phase 1):**
- Quick CLS fix on `menu-buttons` section (5-line CSS rule in `sections/menu-buttons.liquid` or `snippets/css-overrides.liquid`). Estimated +20 desktop home PSI in one commit. File as a `ujg6.5` continuation or a new high-priority ticket. **This single change closes ~half the home CLS gap.**

**Track A (Phase 1 CLS, runs first because it's fastest to value):**
- `ujg6.9` (P1.5 defer 6 CSS) — single file edit, mechanical OC offload
- `ujg6.6` (P1.2 image dimensions) — multi-file mechanical OC offload
- `ujg6.5` (P1.1, scope extended) — done above or in this track
- `ujg6.7`/`ujg6.8` deferred to after Phase 2

**Track B (Phase 2 TBT, runs in parallel where possible):**
- `ujg6.10` (P2.1 audit) — paid-OC long-context dispatch, runs in parallel with Track A
- `ujg6.11` (P2.2 vendor replacement) — sequential after `ujg6.10`
- `ujg6.14` (P2.5 tree-shake) — sequential after `ujg6.10`

**Track C (Phase 3 OS 2.0 uplift, parallel with others):**
- `ujg6.15` (P3.1 TAE migration) — CC admin work, no code conflict
- `ujg6.13` (P2.4 defer apps) — partly admin, partly JS

**Intermediate verification gate (between Track A+B and Track C):**
After Tracks A + B complete, run abbreviated PSI (3 cells: home desktop, brand desktop, collection desktop). If PSI ≥ 65 on each, proceed to Track C completion. If <65, dig deeper before adding more changes.

## 8. Open questions (deferred to Phase 1+)

1. **What is the correct brand collection handle?** `/collections/loreal-professionnel` redirects to `/` — handle has been removed/renamed. Verified via `finalDisplayedUrl` field. Operator action needed: check Shopify admin for the live handle of the L'Oréal Professionnel collection, then update `scripts/psi-baseline-matrix.py` TEMPLATES list before Phase 4.1 verification. **No bd ticket needed — single config change.**
2. **Why does P8 cart desktop score +37 vs P6?** PSI shows P6 cart had a `section-recent` (recently-viewed products) carousel contributing CLS 0.989. P8 cart template omits this. Decision: keep omitting (don't regress this win) OR add a P8-native recently-viewed section if merchandising teams want it back. Out of scope for the epic.
3. **Mobile PDP TBT improved by 3,735ms on P8.** Confirms that the P8 product template architecture is genuinely better. The custom-theme.js cost is offset by other wins on this template. Verify the same dynamic on cart (TBT also better there).
4. **PSI 30s cache: n=3 = n=1.** For Phase 4.1 verification, need to add cache-buster query params (`&_psi=<ts>`) to force fresh runs. Update `scripts/psi-baseline-matrix.py` before verification phase.

## 9. Decision: proceed with Phase 1/2/3 as designed?

**YES with three scope refinements** (above). The diagnosis matches the epic structure with high fidelity:
- TBT bisect implicates the commits the epic already targeted (`c108dc7` and `7d4c71c`)
- CLS attribution implicates the sections the epic already targeted, with one refinement (menu-buttons rises to top priority)
- Three tickets need scope tweaks; no new tickets needed
- One quick-win pre-Phase-1 fix (menu-buttons dimension reservation) can recover ~20 PSI before any other work

**Estimated time to acceptance criteria** (with aggressive OC offload per the recommendations):
- Track 0 (quick win): ~30 minutes CC
- Track A (Phase 1): ~2 days CC + 1 day OC parallel
- Track B (Phase 2): ~3-4 days CC + 1 day OC parallel
- Track C (Phase 3): ~3-4 days CC (TAE admin work) + OC parallel
- Phase 4-5: ~1.5 days CC

**Revised total: ~9-11 CC working days**, in line with the architecture doc's 9-12 day estimate.

## 10. Critical recommendation for the operator

The dev theme is in a transitional hybrid state: **P8 bundle + P6 sections + P6 custom-theme.js**. This was always the plan per the architecture doc; the regression is the cost of that hybridity. **Cutting over to live in this state would be visible in CrUX field data within 28 days**, harming SEO ranking signals.

**The good news from this diagnosis:**
- PDP and cart are already at or better than P6. No work needed there.
- The dominant CLS source is fixable in one tiny CSS edit (menu-buttons reservation).
- The dominant TBT source is removable in 2-3 days of Phase 2 work (custom-theme.js elimination chain).
- Bare P8's 33-point PSI advantage over P6 is recoverable; surpassing P6 with TAE migrations adds ~5-10 more points.

**Recommended immediate action**: file a Track 0 ticket for the `menu-buttons` quick fix and ship it before formally starting Phase 1. The 5-line CSS rule could be in production tonight.
