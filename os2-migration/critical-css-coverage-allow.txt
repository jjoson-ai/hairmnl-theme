# critical-CSS coverage allowlist — scripts/check-critical-css-coverage.py
#
# Format (whitespace-separated; reason runs to end of line):
#   <key>   <bd-id>   <reason>
#
# Keys are SCOPED, not per-class:
#   hero__content__wrapper/brick__block   rule 1 — this class under this late-positioned ancestor
#   navtext/min-width                     rule 2 — this class's declaration of this property
# A bare class token works as a deliberate BLANKET entry, but prefer the scoped form. The same
# class is routinely broken in one context and fine in another — `.hero__content__wrapper` is the
# dh8x bug under `.brick__block` and perfectly safe under `.section--image` — so a blanket entry
# would silently un-guard the broken context.
#
# Prefer an inline `/* ccc-OK: why */` comment in the rule body when the rule itself is the right
# place to record the judgement. Use this file for bulk/baseline entries.
#
# ===============================================================================================
# TRIAGED 2026-08-05 (bd 3q2e). The 22 rows baselined at lint introduction (958r.6) have all been
# individually judged, each verdict independently re-derived by a second analyst instructed to
# refute the first. Outcome: 7 NEEDS_FIX, 4 SAFE, 11 FALSE_POSITIVE.
#
# The three position mirrors below were applied to snippets/critical-css.liquid and their rows
# REMOVED from this file, so the guard is live again on those pairs:
#   /*3q2e*/.double__block{position:relative}          <- hero__content__wrapper/double__block
#           .cross-post-blogs{position:relative}       <- swiper-button-{next,prev}/cross-post-blogs
#           .collection-slider .wrapper{position:relative}  <- swiper-button-{next,prev}/wrapper
# Each is byte-equal to the single unconditional `position:relative` its deferred sheet already
# declares, so it can only remove first-paint shift — it cannot change settled rendering.
#
# BLIND SPOT CLOSED 2026-08-05 (bd gmrm). The lint used to reduce a selector to its rightmost class
# token, so `.collection-slider .wrapper` read as "every .wrapper is positioned". That both invented
# false positives and hid real ones. RULE 1 now carries each rule's requirements (`own` classes,
# `ancestor` classes, `[class*=]` fragments) and resolves the section-schema `class` through
# {% render %} edges, so cross-file ancestry is visible. Five rows were deleted from this file
# because the lint simply no longer reports them:
#   btn-state-complete/uppercase, btn-state-loading/uppercase   (.text-link.uppercase never matches)
#   swiper-button-{next,prev}/swiper-container,
#   swiper-pagination/swiper-container                          (.cross-post-blogs qualifier absent)
# swiper-button-*/grid__item likewise no longer fires (`[class*="push-"]` does not match), and
# image-overlay/cart__empty correctly REAPPEARED — it had been masked by the old over-attribution.
# ===============================================================================================

# --- FALSE POSITIVE: the lint reduces a qualified selector to its rightmost class token, so it
# --- believes an ancestor is positioned when the qualifier never actually matches this markup.
image-overlay/homepage-slideshow    3q2e   FALSE POSITIVE. The overlay that actually ships (section-slideshow.liquid:57) is a child of `.slideshow__slide … section--image`, and critical CSS declares `.section--image{position:relative}` — it already has its containing block. The lint only names `.homepage-slideshow` because of a SECOND occurrence at :102 inside `{% if section.blocks.size == 0 %}`, the theme-editor onboarding placeholder, which is unreachable: all 18 section-slideshow instances have 3-6 blocks and are disabled:true.

# --- SAFE: the element genuinely has no containing block at first paint, but it cannot shift
# --- because it paints nothing.
icon/height                         3q2e   SAFE (medium confidence). `.icon{height:1em}` is byte-identical to its deferred rule — only the inherited em basis moves (html 16px -> body 13.14426px) because critical CSS omits the body font-size. Every first-viewport `.icon` site is either px-pinned in critical CSS byte-identically (`.navlink .icon`, `.header__mobile__* .icon`, `.media__zoom__icon svg`) or sits in a critically height-pinned box (`.tousled-header__wrapper .mobile-menu-box{height:45px}`), leaving <=3px residuals. Do NOT "fix" by adding font-size to `.icon`: `.share__link .icon{1.2em}`, `.pagination-custom .icon{.45em}` and `.announcement__bar--icon .icon{1.1em}` all resolve against the inherited context, so pinning it would permanently change live rendering.

# --- FIXED under bd dwdv by mirroring the body font cascade, but STILL REPORTED: RULE 2 keys on
# --- the flagged class having its OWN font-size, and these classes intentionally have none. The
# --- rows stay so the lint exits 0; they are no longer outstanding work.
collection__nav/height              dwdv   FIXED via the body font mirror (/*dwdv*/ in critical-css.liquid), not outstanding. `height:3.3em` on a bordered, server-rendered nav that is above the fold on every collection page; critical CSS omits the body font-size AND font-family, so first paint is UA serif at 16px (3.3em = 52.8px) and settles to 13.14426px (43.4px) — a ~9.4px reflow. All three deferred blocks were mirrored verbatim (unconditional + @media 480px vw-interpolated + @media 1400px); mirroring only the unconditional block would have changed rendering at every viewport >=480px. Body-scoped, so `input,select,textarea{font-size:16px}` survives as the iOS zoom-on-focus guard. Verified on an unpublished duplicate of live: theme-core.css follows the inline block in document order with byte-identical values, so settled rendering is unchanged; both tiny.content.min.css and theme-core.css are deferred, so the inline rule is the ONLY body font rule at first paint. .collection__nav 52.8px -> 43.4px reflow removed.
icon/width                          dwdv   FIXED at the root by the same body font mirror; the em basis is now correct at first paint. The `.cart__remove__icon` residual on /cart (2.86px) is resolved with it. Two residuals remain by design and are NOT reachable from body: the `.popout__toggle` sort icon and the mobile search button are each pinned by their own 12px accent rules (1.33px each). One larger case is conditional on catalogue data and is filed separately: `.media__thumb .icon` is position:absolute + 20x20 ONLY in the deferred sheet, so on a PDP whose product has video/3D media the icon starts as an in-flow 1em box overflowing the 60x60 thumb.

# ===============================================================================================
# RULE 3 (late-out-of-flow) BASELINE, TRIAGED 2026-08-06 under bd 66t7. 21 findings judged, each
# NEEDS_FIX and each sub-high-confidence call re-derived by a second analyst told to refute the
# first. Outcome: 3 NEEDS_FIX (all fixed below), 12 SAFE, 6 FALSE_POSITIVE. No row is un-vetted.
#
# FIXED, so absent from this file:
#   /*66t7*/.micromodal-slide{display:none}.micromodal-slide.is-open{display:block}
#                                             <- modal__overlay + modal__close. Mirroring the HIDING
#                                                rule is cheaper and safer than mirroring each
#                                                child's position: a display:none modal generates no
#                                                boxes at all. The .is-open twin is mirrored too so a
#                                                modal cannot be stuck hidden if JS beats the sheet.
#   .video-autoplay-wrapper{position:absolute;top:0;height:100%;width:100%}
#
# NOTE ON RULE 3 vs RULE 1: their safety tests are OPPOSITE. A RULE 1 element with no containing
# block teleports into the first viewport, so "below the fold" is NOT a defence. A RULE 3 element
# sits at its natural in-flow position, so "it and everything below it are below the fold" IS a
# valid defence. Several rows rely on that.
#
# THREE MODEL GAPS the triage exposed, all worth knowing before trusting a RULE 3 verdict:
#   1. FIXED in this change — the lint modelled only critical-css.liquid as first-paint CSS and
#      ignored snippets/css-overrides.liquid (~98KB, inline in <head> at layout/theme.liquid:626,
#      Liquid nesting depth 0). That alone produced the product__badge and cart__items__remove
#      false positives. Both sheets are now parsed.
#   2. NOT fixed — the lint assumes every template uses layout/theme.liquid. templates/password.json
#      declares "layout": "password", and layout/password.liquid loads theme.css as a RENDER-BLOCKING
#      link with no critical-css inline and no media=print swap, so the whole deferred-CSS bug class
#      cannot occur there.
#   3. NOT fixed — the lint treats every deferred sheet as loading on every template. Several are
#      Liquid-gated (theme-search.css at layout/theme.liquid:565, theme-cart.css at :556), so a
#      "deferred" rule may simply never apply on the template where the markup lives.
#   4. NOT fixed — build_outer_context propagates a section's SCHEMA class across {% render %}
#      edges, but not markup-level ancestor classes. So a snippet whose containing block is supplied
#      by a wrapper in its calling section looks unpositioned. See cart__items__remove below.
# ===============================================================================================

background-size-cover/out-of-flow   66t7   FALSE POSITIVE. Both occurrences are <img> tags carrying an INLINE style="position:absolute;top:0;left:0;width:100%;height:100%;object-fit:cover" (sections/section-map.liquid:58 and :66). An inline style outranks any class rule, so they are out of flow from the first paint — there is no transition. (The lint reads inline styles only for display/visibility/opacity, not position.)
collection__image__bottom/out-of-flow 66t7 SAFE. Occupies ZERO layout space even while static: its only child is the hover <img>, and critical CSS already gives `.lazy-image img{position:absolute}` — so the div computes to height 0 and collapsing it moves nothing. Independently, it is gated `{% if double_img %}` which needs settings.image_hover_enable, currently false. The zero-height argument survives a merchant flipping that setting.
custom-prev-next-button/out-of-flow 66t7   SAFE on two independent grounds. All 18 section-slideshow instances in templates/ are disabled:true (re-verified, not taken on trust). And even if one were re-enabled the element is an empty div with no critical-CSS box, so it reserves nothing to collapse.
drawer__underlay/out-of-flow        66t7   SAFE. The reported site, sections/api-pickup-availability.liquid, has NO {% schema %} and no template reference — it is fetched only at runtime through the Section Rendering API (assets/theme.js:4823), so it is not part of any initial page load.
drawer__underlay__blur/out-of-flow  66t7   SAFE. An empty <span> with nothing declared for it in first-paint CSS, so the subtree collapses to a zero-height line box; its background only applies in the .drawer--visible state, so it paints nothing on a cold load.
drawer__underlay__fill/out-of-flow  66t7   SAFE. Same deferred rule, same markup shape and same reasoning as drawer__underlay__blur.
gallery__icon/out-of-flow           66t7   SAFE by the below-the-fold exemption, which is valid for RULE 3 (see the note above). Critical CSS has zero occurrences of gallery__icon, so it really is an in-flow inline box at first paint — but on every template that renders it, it and everything beneath it sit outside the first viewport.
helper-note/out-of-flow             66t7   SAFE. Behind a Liquid condition that is false in production: the only live callsite is sections/collection.liquid:105, inside the {% for %}...{% else %} EMPTY branch and additionally gated on collection.handle == 'all'. The 16 bespoke collection-* sections that also include it have zero template references.
hidden-label/out-of-flow            66t7   FALSE POSITIVE, model gap #2. templates/password.json declares "layout": "password", and layout/password.liquid loads theme.css as a render-blocking <link> — it never inlines critical-css.liquid and never uses the media=print swap. The deferred-CSS bug class cannot occur on that layout at all.
image__hero__pane/out-of-flow       66t7   SAFE. Zero layout space at first paint: critical CSS contains no occurrence of image__hero, so the pane has no width/height/padding/border/background of its own, and collapsing a zero-size box moves nothing.
popout-list/out-of-flow             66t7   SAFE (medium confidence). The refutation pass overturned an initial NEEDS_FIX: the claimed ~200px jump on collection.branded-subcollection rested on a misread of sections/brand-collection.liquid:100, where `{%- if request.design_mode or request.page_type == null -%}` takes the ELSE branch on a real pageload.
product-labels/out-of-flow          66t7   FALSE POSITIVE. Every {% render 'product-labels' %} callsite in the repo is commented out (product-grid-item.liquid:261, product-grid-item-branded.liquid:183, section-product.liquid:63), disabled 2026-04-27 under bd spz. WARNING for bd spz: if those renders are ever uncommented this becomes a REAL finding — the label stack would add a line box inside a.lazy-image and push the title/price down. Re-enabling must include `.product-labels{position:absolute;top:8px;left:8px}` in critical CSS.
range__dot/out-of-flow              66t7   SAFE, model gap #3. `.range__dot{position:absolute}` ships only in assets/theme-search.css, which layout/theme.liquid:565 loads only on search templates — so on collection pages, where the filter sidebar also renders, there is no static-to-absolute transition at all.
range__line/out-of-flow             66t7   SAFE. Same widget, same sheet, same template gate as range__dot; and on the search template the ancestor .collection__filters__inner is opacity:0 at first paint.
spinner-wrapper/out-of-flow         66t7   FALSE POSITIVE. Dead code: sections/collection-branded.liquid has zero template references and no presets block (bd nlr8 recorded the same). The deferred rule is also the DESCENDANT selector `.collection-slider .spinner-wrapper`, and that section carries no such schema class.
tousled-logo/out-of-flow            66t7   SAFE, doubly. The <img> is gated `{% if tousled_logo != blank %}`, and its ancestor .sliderule__wrapper is a slide-out menu that is not open at first paint.

# ===============================================================================================
# CROSS-FILE SUPPRESSION ADDED 2026-08-06 (bd nf3j). A snippet sits inside whatever markup encloses
# its render callsite, so a containing block can come from a wrapper in the CALLING file:
# css-overrides positions `.cart__template .cart__items__row`, but `.cart__template` is on a wrapper
# in sections/cart.liquid, one render above snippets/cart-line-items.liquid.
#
# That context is used for SUPPRESSION ONLY, never detection, and the asymmetry is deliberate. The
# union across callers is a fiction: snippets/icon-bin.liquid is rendered both from the footer and
# from inside .cart__items__remove, so a merged set satisfies `.footer__title .icon{position:absolute}`
# from one caller while offering `.cart__items__remove` as the late ancestor from another - an
# ancestry that never co-occurs on a real page. Feeding it to the detection side invented three
# findings when tried, so detection stays on the schema-class context, which is deterministic (one
# class per section). Being over-broad on the suppression side can only remove a finding, never
# invent one. Per-caller contexts were also tried and made things worse, not better.
#
# Model gap #4 in the header above is now CLOSED. Rows deleted because the lint stopped reporting
# them: image-overlay/cart__empty, item--loadbar/cart__items__row, tabs__arrow/tabs__head, cart__items__remove/out-of-flow, cart__items__remove/cart__items__row
# ===============================================================================================

# ===============================================================================================
# RULE 4 (late-size-or-display) BASELINE, TRIAGED 2026-08-06 under bd cuzo. 30 findings judged in
# 5 groups, every NEEDS_FIX and every sub-high-confidence call re-derived by a second analyst told
# to refute the first. Outcome: 5 NEEDS_FIX (all fixed below), 8 SAFE, 13 FALSE_POSITIVE, plus 4
# that surfaced after the triage groups were formed and are judged inline in their rows.
#
# FIXED — five findings, TWO mirrors, both with load-bearing constraints:
#   /*cuzo*/@media only screen and (max-width:767px){.cart__heading__wrapper .cart__items__price,
#           .cart__heading__wrapper .cart__items__quantity{display:none}}
#     The @media wrapper MUST stay: at >=768px cart-page.css gives the cart a five-column grid where
#     those headers are deliberately visible, so an unscoped mirror would permanently delete the
#     desktop Price and Quantity column headings.
#   .cart__footer__update{display:none}
#     NO !important. `.no--ajax|.no-js .cart__footer__update{display:flex}` is (0,2,0) and must keep
#     beating this (0,1,0) mirror — it is the only quantity-update control on the JS-disabled path.
#     This single mirror also retires cart__update and cart__update__icon: with their ancestor hidden
#     at first paint they cannot paint, which the lint's hidden-ancestor exemption already honours.
#
# WHAT RULE 4 STILL CANNOT SEE, recorded so nobody assumes otherwise: classes that reach the DOM
# through a Liquid variable. fj5m's own `.cart--hidden` is emitted as class="{{ empty_init }}", so
# strip_liquid erases it and the markup walker never sees the class at all.
# ===============================================================================================

additional-checkout-buttons/min-height-late    cuzo   SAFE. Defence (d) — never rendered on this store. Both callsites are behind `{% if additional_checkout_buttons and settings.cart_show_additional_buttons %}`, and `cart_show_additional_buttons` is false: it is absent from config/settings_data.json entirely (grep returns zero hits), so Liquid falls back to the schema default, which is explicitly `false`. The schema's own info string even says the option is 'Not reccomended for drawer or popup style carts', and this store runs `"cart_style": "drawer"` (config/settings_data.json:66) — consistent with it never having been switched on. The `additional_che
announcement__wrapper/display-late             cuzo   FALSE_POSITIVE. The two deferred rules both REQUIRE a `.desktop` or `.mobile` class that the shipping wrapper never carries, and the two divs that would carry it are behind a Liquid gate that is false in every stored config. Condition (d) + (e). sections/announcement.liquid emits three mutually-exclusive wrappers: the bare one at :354, `.announcement__wrapper desktop` at :386 gated on `{%- if bar_markup_desktop != '' -%}` (:385), and `.announcement__wrapper mobile` at :418 gated on `{%- if bar_markup_mobile != '' -%}` (:417). Those two buckets are only appended to inside `{%- if block.settings.target_device_e
brick__product/display-late                    cuzo   FALSE_POSITIVE. The deferred selector is qualified on a `.brick__collection` ancestor, and `.brick__collection` is never rendered: it exists only inside the `{% when 'collection' %}` branch of the two brick sections, and NO template in the repo instantiates a `collection` block. The `.brick__product` instances that DO render (from `product` blocks) have no `.brick__collection` ancestor, so the deferred rule cannot match them (harmless criterion e + d).
cart__graph/display-late                       cuzo   SAFE. Two independent defences, either sufficient. (d) NEVER RENDERED IN THIS SELECTOR CONTEXT: the deferred rule is `.announcement__text .cart__graph`, and `.cart__graph` only appears inside the announcement section's `{%- when 'message' -%}` branch (sections/announcement.liquid:91), i.e. the 'Free shipping message' block type (schema block `"type": "message"`, sections/announcement.liquid:560). This store's announcement section has FOUR blocks and every one is `"type": "text"` — three of them additionally `"disabled": true`, leaving one active text block. No message block exists, so no `.cart__gra
collection-filters/display-late                cuzo   SAFE. The element is never in the render tree at first paint on any live surface. The only template-reachable emitter is sections/brand-collection.liquid, and on a normal pageload that section takes its ELSE branch and emits only a lazy placeholder; the real markup is inside a <noscript>, which is not parsed into DOM when scripting is enabled. The ~30 other sections that emit `.collection-filters` have zero template references. Confirmed against the live storefront. Also note the deferred rule is media-scoped, so a naive mirror is actively harmful.
collection__image__bottom/display-late         cuzo   FALSE_POSITIVE. The div is behind a Liquid condition that is false in production: `double_img` requires `settings.image_hover_enable`, which is false in the `current` block of config/settings_data.json. Confirmed by zero occurrences in live HTML across three collection templates and search. Defence (d), never rendered. NOTE: the bd 66t7 reasoning quoted in the brief does NOT carry over as stated — see evidence.
desktop/display-late                           cuzo   FALSE_POSITIVE. Duplicate of the announcement__wrapper finding, re-keyed by the other class token in the compound selector `.announcement__wrapper.desktop`. RULE 4's `rule_matches` requires `own ⊇ {announcement__wrapper}`, so a bare `.desktop` elsewhere could never match — and there is no other element in the theme carrying `desktop` as a standalone class alongside `announcement__wrapper` (grep across sections/, snippets/, layout/ for `class="...desktop..."` returns only announcement.liquid:386, plus unrelated `desktop-only` / `header__desktop` / `slides-mobile-*` tokens). Same condition (d): the div is behin
desktop-only/display-late                      cuzo   SAFE. Condition (c): the element sits at its natural in-flow position and it and everything below it are outside the first viewport on every template that renders it. `.desktop-only` is only referenced by sections/brand-collection.liquid (26 hits) and by sections/collection-branded.liquid plus 27 `sections/collection-<brand>.liquid` files that NO template references — a scan of all templates/*.json plus the 6 templates/*.liquid found only `collection-hero` (84), `collection-sub` (84) and `brand-collection` (31) as live section types, and the sole `{% section %}` tag in a .liquid template is `gift-ca
drawer__underlay/display-late                  cuzo   SAFE. Two independent grounds. (1) The element occupies no space at first paint: it is an empty `<span>` containing two empty `<span>`s, and EVERY box property it has — position:fixed, height:100%, width:100%, opacity:0, visibility:hidden, and the children's display:block/position:absolute — lives in the SAME deferred sheet as the flagged display:none. No first-paint sheet declares anything for `.drawer__underlay*`. So the transition is `0-height inline box` -> `display:none` (<=479px) or `position:fixed, out of flow` (>479px); in-flow geometry never changes (criterion b). (2) The specific occurrenc
hidden-md-down/display-late                    cuzo   FALSE_POSITIVE. The critical/deferred architecture the rule is premised on does not apply to the only page that renders this element. `.hidden-md-down` appears exactly once in the whole theme: sections/password.liquid:101 `<span class="hidden-md-down">|</span>`. templates/password.json declares `"layout": "password"` (:20), and layout/password.liquid does NOT inline snippets/critical-css.liquid and does NOT use the media=print deferral — it loads the pre-split monolith render-blocking at :82 via `{{ 'theme.css' | asset_url | stylesheet_tag }}`. assets/theme.css:7176-7180 contains the same `@media only screen 
image__hero__scale/min-height-late             cuzo   FALSE_POSITIVE. The `[data-parallax-wrapper]` attribute the selector requires is emitted only under `{% if settings.parallax_enable %}`, and the theme setting is false — the attribute appears in exactly one place in the whole theme and is currently never output, so the deferred rule never matches (criterion e). Second, independent ground: even with parallax on, the same Liquid branch writes an inline `style="height:...vw"` onto `.image__hero__scale`, and `min-height:110%` would resolve against `.image__hero__pane`, whose `height:100%` is itself deferred — a percentage min-height against an auto-height parent 
mobile/display-late                            cuzo   FALSE_POSITIVE. Duplicate of the announcement__wrapper finding, re-keyed by the `mobile` token in `.announcement__wrapper.mobile`. Note this one differs from its `.desktop` sibling in a way that matters for any future fix: the `display:none` here is UNCONDITIONAL (theme-core.css:10843), paired with a `display:block` override inside `@media only screen and (max-width: 767px)` (:10846-10850). It is still a false positive for the same reason — sections/announcement.liquid:418 is behind `{%- if bar_markup_mobile != '' -%}` (:417) and the bucket is only filled inside `{%- if block.settings.target_device_enabled -%
mobile-only/display-late                       cuzo   SAFE. Condition (c), identical analysis to `.desktop-only` — same file, same `{% for block in section.blocks %}` loop, same 31 live templates. Worth recording the settled cascade because two sheets disagree: assets/theme-core.css:9029-9033 hides it only at `min-width: 480px`, while assets/custom-theme-core.css:1877 hides it UNCONDITIONALLY and :1908-1912 re-shows it at `max-width: 768px`. custom-theme-core.css loads after theme-core.css (layout/theme.liquid:537 then :539), so the settled behaviour is: visible <=768px, hidden >768px. At first paint, with neither sheet applied, a div defaults to displ
pb-text-excerpt/display-late                   cuzo   SAFE. I REFUTE their load-bearing argument (criterion d, "unreachable dead code") but cannot refute the verdict, which survives on criterion (c) alone. WHAT THEY GOT WRONG: they treated the live app markup as unverifiable and leaned on the dead theme snippet. It is verifiable, and the answer is that the live Pro Blogger theme-app-extension DOES emit `.pb-text-excerpt`, server-rendered, with non-empty text. Prior sessions left real live-storefront captures in this session's scratchpad, and every article capture contains exactly 6 `.pb-text-excerpt` nodes each holding a truncated excerpt sentence. So 
scroll_link/height-late                        cuzo   FALSE_POSITIVE. Never rendered (criterion d) — bd ov1z's argument re-verified and it does hold for RULE 4. All three markup sites are gated on `section.settings.height == 'use_screen_full'`; only two templates set that value anywhere and both set it on `section-double` sections, which contain no scroll link. The schema default for all three sections is `use_screen_two_thirds`, so sections that omit the setting do not render it either. Second, independent ground: critical CSS already gives `.scroll_link` position:absolute, so even if it were rendered the box is out of flow at first paint and a late height:60px
shop-url/display-late                          cuzo   FALSE_POSITIVE. The critical/deferred split does not exist on the only page that renders this element. `.shop-url` appears once, in sections/gift-card.liquid, used only by templates/gift_card.liquid — a `{% layout none %}` template with its own <head> that loads the stylesheet render-blocking via `stylesheet_tag`, not via the `media=print` swap. So `display:none` is in effect before first paint. The finding is an artifact of the lint treating the pre-split monolith `assets/theme.css` as deferred (DEFERRED_GLOBS keeps `theme.css` 'for reference'), which is true for layout/theme.liquid pages but false for this 
toggle-filters/display-late                    cuzo   SAFE. Same mechanism and same defence as .collection-filters: the only template-reachable emitter is sections/brand-collection.liquid, whose storefront path emits a lazy placeholder and puts the real markup inside <noscript>. Verified in live HTML. The deferred rule is also the inverse responsive utility (desktop hide, min-width:769px), so an unconditional mirror would break mobile filtering rather than fix a shift.
upsell__content/min-height-late                cuzo   FALSE_POSITIVE. Defence (d) — never rendered, on two independent grounds. (1) The file the checker attributes the finding to, snippets/upsell-product-list.liquid, has exactly one {% render %} callsite in the whole repo and it sits inside a {% comment %} block: sections/product.liquid:213 is `{% comment %} {% when 'upsell_list' %}` and :214 is `{% render 'upsell-product-list' ... %} {% endcomment %}`. sections/section-product.liquid has no upsell_list case at all. The snippet is dead code. (2) The sibling live snippet snippets/upsell-product.liquid (which emits the same .upsell__content markup at line 44) is w
upsell__image/height-late                      cuzo   FALSE_POSITIVE. Same defence (d) as upsell__content — the element is never rendered on the storefront. snippets/upsell-product-list.liquid:25 (`<div class="upsell__image">`) is in a snippet whose only {% render %} is commented out at sections/product.liquid:214; snippets/upsell-product.liquid:24 is inside the `{%- unless upsell_error -%}` guard that is always false on the storefront because all six templates instantiating the `upsell` block set product to "". Secondary point even if it did render: the inner <img> carries literal width="90" height="90" attributes (snippets/upsell-product.liquid:35-36 and snipp
upsell__image__inner/height-late               cuzo   FALSE_POSITIVE. Same defence (d) — never rendered. .upsell__image__inner appears only at snippets/upsell-product-list.liquid:28/31/40 (dead snippet, render callsite inside {% comment %} at sections/product.liquid:213-214) and snippets/upsell-product.liquid:27/30/39 (inside the always-true `upsell_error` short-circuit on the storefront). Additionally, the class is applied to the <img> itself in the normal branch, and that <img> already has width="90" height="90" HTML attributes, so its box is reserved at first paint independently of theme-core.css.
upsell__variant-title/display-late             cuzo   FALSE_POSITIVE. Same defence (d) — never rendered — plus a second independent defence in the live snippet. (1) The flagged file snippets/upsell-product-list.liquid:52 is dead code (only {% render %} is inside {% comment %} at sections/product.liquid:213-214). (2) In the live snippet snippets/upsell-product.liquid the <p class="upsell__variant-title"> at line 52 is doubly gated: it sits inside the storefront-false `{%- unless upsell_error -%}` (line 16) AND inside `{%- unless upsell_product.has_only_default_variant -%}` (line 51) — and the block's own schema paragraph in sections/product.liquid states "Upsell 
drawer__top/display-late                       cuzo   SAFE. The deferred rule is the qualified `.collection__filters__wrapper .drawer__top` — the wrapper is the filter drawer's own container, so the rule only ever applies inside a drawer that is itself hidden at first paint (critical CSS gives .drawer__content position:fixed + visibility:hidden). Nothing paints, so nothing can collapse.
newsletter__message/display-late               cuzo   SAFE. The deferred selector requires `.has-error`, a state class JS adds only after a failed newsletter submit. At first paint that state does not exist, so the display:none never applies and there is no visible-then-hidden transition.
product-description__actions/display-late      cuzo   SAFE. Multi-selector rule shared with `.accordion-content__actions`; the actions row lives inside a collapsed accordion panel that is not expanded at first paint, so it occupies no flow space to collapse.
show-filters/display-late                      cuzo   SAFE. The deferred selector requires `.drawer--visible` or `.filters--default-visible` — both are runtime state classes (the first is added when the filter drawer opens). Neither exists at first paint, so the rule cannot fire before the deferred sheet has landed.
