# critical-CSS coverage allowlist — scripts/check-critical-css-coverage.py
#
# Format (whitespace-separated; reason runs to end of line):
#   <key>   <bd-id>   <reason>
#
# Keys are SCOPED, not per-class:
#   hero__content__wrapper/brick__block   rule 1 — this class under this late-positioned ancestor
#   navtext/min-width                     rule 2 — this class's declaration of this property
# A bare class token works as a deliberate BLANKET entry, but prefer the scoped form. The same
# class is routinely broken in one context and fine in another — `.hero__content__wrapper` is the
# dh8x bug under `.brick__block` and perfectly safe under `.section--image` — so a blanket entry
# would silently un-guard the broken context.
#
# Prefer an inline `/* ccc-OK: why */` comment in the rule body when the rule itself is the right
# place to record the judgement. Use this file for bulk/baseline entries.
#
# ===============================================================================================
# TRIAGED 2026-08-05 (bd 3q2e). The 22 rows baselined at lint introduction (958r.6) have all been
# individually judged, each verdict independently re-derived by a second analyst instructed to
# refute the first. Outcome: 7 NEEDS_FIX, 4 SAFE, 11 FALSE_POSITIVE.
#
# The three position mirrors below were applied to snippets/critical-css.liquid and their rows
# REMOVED from this file, so the guard is live again on those pairs:
#   /*3q2e*/.double__block{position:relative}          <- hero__content__wrapper/double__block
#           .cross-post-blogs{position:relative}       <- swiper-button-{next,prev}/cross-post-blogs
#           .collection-slider .wrapper{position:relative}  <- swiper-button-{next,prev}/wrapper
# Each is byte-equal to the single unconditional `position:relative` its deferred sheet already
# declares, so it can only remove first-paint shift — it cannot change settled rendering.
#
# BLIND SPOT CLOSED 2026-08-05 (bd gmrm). The lint used to reduce a selector to its rightmost class
# token, so `.collection-slider .wrapper` read as "every .wrapper is positioned". That both invented
# false positives and hid real ones. RULE 1 now carries each rule's requirements (`own` classes,
# `ancestor` classes, `[class*=]` fragments) and resolves the section-schema `class` through
# {% render %} edges, so cross-file ancestry is visible. Five rows were deleted from this file
# because the lint simply no longer reports them:
#   btn-state-complete/uppercase, btn-state-loading/uppercase   (.text-link.uppercase never matches)
#   swiper-button-{next,prev}/swiper-container,
#   swiper-pagination/swiper-container                          (.cross-post-blogs qualifier absent)
# swiper-button-*/grid__item likewise no longer fires (`[class*="push-"]` does not match), and
# image-overlay/cart__empty correctly REAPPEARED — it had been masked by the old over-attribution.
# ===============================================================================================

# --- FALSE POSITIVE: the lint reduces a qualified selector to its rightmost class token, so it
# --- believes an ancestor is positioned when the qualifier never actually matches this markup.
image-overlay/homepage-slideshow    3q2e   FALSE POSITIVE. The overlay that actually ships (section-slideshow.liquid:57) is a child of `.slideshow__slide … section--image`, and critical CSS declares `.section--image{position:relative}` — it already has its containing block. The lint only names `.homepage-slideshow` because of a SECOND occurrence at :102 inside `{% if section.blocks.size == 0 %}`, the theme-editor onboarding placeholder, which is unreachable: all 18 section-slideshow instances have 3-6 blocks and are disabled:true.

# --- SAFE: the element genuinely has no containing block at first paint, but it cannot shift
# --- because it paints nothing.
image-overlay/cart__empty           3q2e   SAFE. The chain .wrapper -> .cart__empty really is unpositioned at first paint, but the element paints nothing: critical CSS never DEFINES `--bg` (19 var(--bg) reads, zero definitions, no @property), so `background-color:var(--bg)` is invalid-at-computed-value-time and falls back to transparent; the div is server-rendered empty with no border, shadow, image or content-bearing pseudo. FRAGILE, not broken: if anyone later adds a `:root{--bg:...}` block to critical-css.liquid this becomes an opaque full-viewport rectangle at z-index 2. The cheap hardening is `.cart__empty{position:relative}` — byte-consistent with theme-core.css:14654 and rendering-neutral.
item--loadbar/cart__items__row      3q2e   SAFE. Server-rendered with an inline `style="display: none;"` (cart-line-items.liquid:7), and NO stylesheet anywhere declares `display` for `.item--loadbar`, so the inline rule wins at first paint and neither the bar nor its background-carrying `:before` generates a box. A display:none element cannot shift.
tabs__arrow/tabs__head              3q2e   SAFE. Every `.tabs__arrow` in the theme ships with the `is-hidden` class, and critical CSS carries `.is-hidden{visibility:hidden;max-height:0;overflow:hidden;opacity:0}` unconditionally with nothing re-asserting visibility or opacity. The lint is structurally right that `.tabs__head` is uncovered (the critical build kept only the `.tabs-wrapper` half of the deferred `.tabs-wrapper,.tabs__head` selector list) — but an invisible element contributes no shift. Optional belt-and-braces mirror `.tabs__head{position:relative}` is byte-safe if ever wanted.
icon/height                         3q2e   SAFE (medium confidence). `.icon{height:1em}` is byte-identical to its deferred rule — only the inherited em basis moves (html 16px -> body 13.14426px) because critical CSS omits the body font-size. Every first-viewport `.icon` site is either px-pinned in critical CSS byte-identically (`.navlink .icon`, `.header__mobile__* .icon`, `.media__zoom__icon svg`) or sits in a critically height-pinned box (`.tousled-header__wrapper .mobile-menu-box{height:45px}`), leaving <=3px residuals. Do NOT "fix" by adding font-size to `.icon`: `.share__link .icon{1.2em}`, `.pagination-custom .icon{.45em}` and `.announcement__bar--icon .icon{1.1em}` all resolve against the inherited context, so pinning it would permanently change live rendering.

# --- FIXED under bd dwdv by mirroring the body font cascade, but STILL REPORTED: RULE 2 keys on
# --- the flagged class having its OWN font-size, and these classes intentionally have none. The
# --- rows stay so the lint exits 0; they are no longer outstanding work.
collection__nav/height              dwdv   FIXED via the body font mirror (/*dwdv*/ in critical-css.liquid), not outstanding. `height:3.3em` on a bordered, server-rendered nav that is above the fold on every collection page; critical CSS omits the body font-size AND font-family, so first paint is UA serif at 16px (3.3em = 52.8px) and settles to 13.14426px (43.4px) — a ~9.4px reflow. All three deferred blocks were mirrored verbatim (unconditional + @media 480px vw-interpolated + @media 1400px); mirroring only the unconditional block would have changed rendering at every viewport >=480px. Body-scoped, so `input,select,textarea{font-size:16px}` survives as the iOS zoom-on-focus guard. Verified on an unpublished duplicate of live: theme-core.css follows the inline block in document order with byte-identical values, so settled rendering is unchanged; both tiny.content.min.css and theme-core.css are deferred, so the inline rule is the ONLY body font rule at first paint. .collection__nav 52.8px -> 43.4px reflow removed.
icon/width                          dwdv   FIXED at the root by the same body font mirror; the em basis is now correct at first paint. The `.cart__remove__icon` residual on /cart (2.86px) is resolved with it. Two residuals remain by design and are NOT reachable from body: the `.popout__toggle` sort icon and the mobile search button are each pinned by their own 12px accent rules (1.33px each). One larger case is conditional on catalogue data and is filed separately: `.media__thumb .icon` is position:absolute + 20x20 ONLY in the deferred sheet, so on a PDP whose product has video/3D media the icon starts as an in-flow 1em box overflowing the 60x60 thumb.
