{% comment %}
  Stub — delegates to the shared product-grid-item-branded snippet.
  Sets show_soldout = true so the sold-out label renders (hidden in the
  main branded template where inventory is always high).
  Font params (product_title_font, product_title_font_size,
  product_price_font, product_price_font_size) flow through from the
  calling section scope automatically via include.
{% endcomment %}
{% assign show_soldout = true %}
{% include 'product-grid-item-branded' %}
