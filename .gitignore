.shopifyignore
.env
*.log
.DS_Store
node_modules/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Claude Code worktrees (per-session, not project state)
.claude/worktrees/

# Python bytecode
__pycache__/

# session/auth artifacts (phase4 audit 2i8b.88 — never commit browser session state)
.auth/

# Generated perf-dashboard artifacts — tracked on the `dashboard-data` branch
# (CI commits refreshes there; pages.yml overlays it at deploy time) and
# regenerated locally by launchd (com.hairmnl.daily-perf). Untracked on main
# 2026-08-15: bot commits to main + local regeneration of the same tracked
# files caused a rebase conflict in three separate sessions.
dashboard/index.html
dashboard/data/snapshots.jsonl
dashboard/data/archive/
